using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TrackerSet : MonoBehaviour
{
    // Stores the type of trackes of this set
    public TrackerSystemsMannager.TYPE_TRACKER typeTracker;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
