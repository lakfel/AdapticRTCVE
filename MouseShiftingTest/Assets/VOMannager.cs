using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class VOMannager : MonoBehaviour
{
    [Tooltip("Positions to be used originally on VR. 0-Left and 1-Rigth")]
    public Transform[] originalPositions;

    [Tooltip("Virtual objects representation")]
    public GameObject[] VObjects;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
